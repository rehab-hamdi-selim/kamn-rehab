import 'package:flutter/material.dart';

class CustomSearching extends StatelessWidget {
  const CustomSearching({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
