import 'package:flutter/material.dart';

class CustomGymFeaturesSection extends StatelessWidget {
  const CustomGymFeaturesSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const  Center(child: Text("Gym Features Content"));
  }
}