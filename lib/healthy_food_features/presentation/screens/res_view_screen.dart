import 'package:flutter/material.dart';

class ResViewScreen extends StatelessWidget {
  const ResViewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
